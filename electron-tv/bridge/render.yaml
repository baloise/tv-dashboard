services:
  - type: web
    name: tv-bridge
    runtime: node
    rootDir: electron-tv/bridge
    buildCommand: npm install --omit=dev
    startCommand: node server.js
    plan: free
    region: frankfurt
    healthCheckPath: /health
    envVars:
      - key: SHARED_TOKEN
        sync: false
